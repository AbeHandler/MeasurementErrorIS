\documentclass{article}
\usepackage{tikz}

\begin{document}

\section{Qiao 2025}
\begin{itemize}
\item Average value of a variable $\bar{X}_i$.
\item Average value of a proxy $\bar{W}_i$ (e.g. from a classifier)
\item The true model $p(Y \vert \bar{X}, Z, \theta)$
\item Also assumes measurement error term does not correlate with the dependent variable.
\item Assumes you have a validation dataset and can estimate error on that dataset.
\item $M_i$$\bar{W}_i$ is number of predicted positives
\item P(TP) is TP rate

\item why is (3) not an integral? I think it is $2^{M_i}$ possible?
\end{itemize}

\end{document}
